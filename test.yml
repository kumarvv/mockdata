target:
  type: json
  to_path: /Users/kumarvv/dev/temp

tables:
  - name: tags
    mode: append
    row_count: 10
    columns:
      - name: uuid
        type: uuid
      - name: name
        type: random_full_name
      - name: description
        type: random_paragraph
      - name: is_active
        type: boolean
        value: true
